<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Track Evolution</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- The auth callback. `GET /auth/login?client=app` runs OAuth in the system
	     browser and redirects back to trackevolution://auth with a single-use
	     code (src/routes/auth.ts). Keep in sync with
	     TrackEvolutionKit.callbackScheme. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>app.trackevolution.auth</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>trackevolution</string>
			</array>
		</dict>
	</array>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- CarPlay driving-task scene (NS-19). Only the CarPlay role is declared: a
	     SwiftUI-lifecycle app supplies its own window scene, and naming one here
	     would replace it. (The Capacitor app had to declare a phone scene by hand
	     for exactly the opposite reason — it was on the app-delegate lifecycle, and
	     the manifest's presence moved it onto scenes.)

	     iOS attaches this scene only to a build signed with
	     com.apple.developer.carplay-driving-task, which Apple has granted and which
	     is now in TrackEvolution.entitlements. Drop the key and this manifest stays
	     harmless — the scene simply never attaches, and no test notices, which is
	     why CI checks the key is present. See the README's CarPlay section. -->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<true/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>CPTemplateApplicationSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>CPTemplateApplicationScene</string>
					<key>UISceneConfigurationName</key>
					<string>CarPlay</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).CarPlaySceneDelegate</string>
				</dict>
			</array>
		</dict>
	</dict>
	<!-- The app only uses standard HTTPS (exempt encryption), so App Store
	     Connect skips the export-compliance prompt on every upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Live lap recorder: GPS keeps recording with the phone locked/stowed
	     during a track session. Only active while the user is recording.
	     App Review-approved copy, carried over verbatim from the Capacitor app. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Your location is recorded during a track session to time your laps from GPS.</string>
	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>Your location is recorded during a track session — including with the screen locked — to time your laps from GPS.</string>
	<!-- Precise Location off puts fixes hundreds of metres out, which no amount
	     of gate math rescues — so the recorder can ask for it for one session.
	     The key is LocationService.temporaryAccuracyPurposeKey. -->
	<key>NSLocationTemporaryUsageDescriptionDictionary</key>
	<dict>
		<key>recordLaps</key>
		<string>Lap timing needs precise location — without it, GPS can't tell which side of the start/finish line you're on.</string>
	</dict>
	<key>UIBackgroundModes</key>
	<array>
		<string>location</string>
	</array>
	<!-- Video telemetry import (NS-30). Reading a clip's telemetry track without
	     copying the whole file needs a PHAsset, which needs a photo-library read
	     authorization; PhotosPicker's loadTransferable would avoid the prompt and
	     copy gigabytes instead. Files import needs no permission at all. -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Track Evolution reads the telemetry track of a video you pick to work out your lap times. The video itself stays on your phone and is never uploaded or copied.</string>
	<!-- "Open with Track Evolution" from Files, Photos and the GoPro app. Paired
	     with LSSupportsOpeningDocumentsInPlace so a multi-GB clip is read where it
	     sits rather than copied into the app's Inbox first. -->
	<key>LSSupportsOpeningDocumentsInPlace</key>
	<true/>
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>Track video</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>LSHandlerRank</key>
			<string>Alternate</string>
			<key>LSItemContentTypes</key>
			<array>
				<string>public.mpeg-4</string>
				<string>com.apple.quicktime-movie</string>
			</array>
		</dict>
	</array>
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Lets dev builds on a phone reach `npm run dev` on the Mac over
		     plain http (LAN). Https traffic is unaffected. -->
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- Dark launch background (#0a0a0b) matching the web app's shell, so the
	     handoff from launch to first frame doesn't flash white. -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UILaunchScreenBackgroundColorName</key>
		<string>LaunchBackground</string>
	</dict>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
