<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Track Evolution</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- The auth callback. `GET /auth/login?client=app` runs OAuth in the system
	     browser and redirects back to trackevolution://auth with a single-use
	     code (src/routes/auth.ts). Keep in sync with
	     TrackEvolutionKit.callbackScheme. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>app.trackevolution.auth</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>trackevolution</string>
			</array>
		</dict>
	</array>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- The app only uses standard HTTPS (exempt encryption), so App Store
	     Connect skips the export-compliance prompt on every upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Live lap recorder: GPS keeps recording with the phone locked/stowed
	     during a track session. Only active while the user is recording.
	     App Review-approved copy, carried over verbatim from the Capacitor app. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Your location is recorded during a track session to time your laps from GPS.</string>
	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>Your location is recorded during a track session — including with the screen locked — to time your laps from GPS.</string>
	<!-- Precise Location off puts fixes hundreds of metres out, which no amount
	     of gate math rescues — so the recorder can ask for it for one session.
	     The key is LocationService.temporaryAccuracyPurposeKey. -->
	<key>NSLocationTemporaryUsageDescriptionDictionary</key>
	<dict>
		<key>recordLaps</key>
		<string>Lap timing needs precise location — without it, GPS can't tell which side of the start/finish line you're on.</string>
	</dict>
	<key>UIBackgroundModes</key>
	<array>
		<string>location</string>
	</array>
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Lets dev builds on a phone reach `npm run dev` on the Mac over
		     plain http (LAN). Https traffic is unaffected. -->
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- Dark launch background (#0a0a0b) matching the web app's shell, so the
	     handoff from launch to first frame doesn't flash white. -->
	<key>UILaunchScreen</key>
	<dict>
		<key>UILaunchScreenBackgroundColorName</key>
		<string>LaunchBackground</string>
	</dict>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
