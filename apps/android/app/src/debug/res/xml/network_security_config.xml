<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug builds only. `wrangler dev` serves plain HTTP, and the server's
    DEV_MODE sign-in bypass answers on 10.0.2.2 — the emulator's alias for the
    host machine — so a debug build pointed there gets one-tap local sign-in.

    Scoped to those two hosts rather than a blanket usesCleartextTraffic, and in
    the debug source set rather than main, so there is no path by which a release
    build ships a cleartext exception.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
