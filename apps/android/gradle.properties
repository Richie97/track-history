org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

kotlin.code.style=official

# Required by the Android Gradle plugin; AndroidX is the only support library
# the native client uses.
android.useAndroidX=true
# Each module gets its own R class rather than inheriting its dependencies'
# resources — smaller builds and no accidental cross-module resource references.
android.nonTransitiveRClass=true
